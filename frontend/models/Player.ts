export interface Player {
	id: Number,
  name: String,
	sex: Number,
	vocation: Number,
	level: Number,
	maglevel: Number,
	lastlogin: Number,
	healthmax: Number,
	manamax: Number,
	online: Number,
	created: Number
}

export interface Skill {
	skillid: Number,
	value: Number
}